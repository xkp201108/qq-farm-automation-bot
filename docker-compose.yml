version: '3.8'

services:
  qq-farm-bot:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - TZ=Asia/Shanghai
    volumes:
      - ./core/data:/app/core/data
    restart: unless-stopped
