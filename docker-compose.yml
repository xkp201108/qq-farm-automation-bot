services:
  qq-farm-bot:
    build: .
    container_name: qq-farm-bot
    restart: always
    ports:
      - "3000:3000"
    volumes:
      # 重要：挂载 data 目录，防止更新镜像后账号信息丢失
      - ./data:/app/core/data
    environment:
      - TZ=Asia/Shanghai
      - ADMIN_PORT=3000
